"use strict";

var React = require('react');

var Trips = React.createClass({
  render: function(){
    return (
      <div className="container">
        <h1>Trips</h1>
      </div>
    );
  }
});

module.exports = Trips;
